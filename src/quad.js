export const vertices = new Float32Array([
  1.0, 1.0, // top right
  -1.0, 1.0, // top left
  1.0, -1.0, // bottom right
  -1.0, -1.0, // bottom left
]);

export const indices = new Uint16Array([
  0, 1, 2,
  1, 3, 2,
]);
